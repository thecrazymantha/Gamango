/*
import { FlowRouter } from 'meteor/kadira:flow-router';
import { BlazeLayout } from 'meteor/kadira:blaze-layout';
// Ce fichier javascript contient les routes qui constitueront le point de départ pour
// le développement de notre application.

// Tout ce qu'il permet pour le moment est d'accéder à la page html que vous codez en
// tapant "/" suivi du nom du template (que j'ai prédéfini). Il faut donc soit nommer
// votre template accordement ou modifier la route (après le "action et render")


// P.S J'ai déja ajouté le package FlowRouter et blaze layout


// Route pour l'écran principal (probablement le login)

FlowRouter.route('/', {
  name: 'home',
  action() {
    BlazeLayout.render('HOME');
  },
});


// Route pour le menu (c-à-d l'endroit où l'utilisateur choisit entre la map,
// médailles et le classement)

FlowRouter.route('/MENU', {
  name: 'Menu',
  action() {
    BlazeLayout.render('MENU');
  },
});


// Route pour la map (score + map)
FlowRouter.route('/MAP', {
  name: 'home',
  action() {
    BlazeLayout.render('score', { main: 'canvas' });
  },
});

// Route pour les templates de login
// eslint-disable-next-line max-len
// eslint-disable-next-line max-len


FlowRouter.route('/LOGIN', {
  name: 'Login',
  action() {
    BlazeLayout.render('login');
  },
});
FlowRouter.route('/NEW-ACCOUNT', {
  name: 'New-account',
  action() {
    BlazeLayout.render('signup');
  },
});

FlowRouter.route('/TEST', {
  name: 'TEST',
  action() {
    BlazeLayout.render('score', { main: 'TEST' });
  },
});


// Route pour les médailles
FlowRouter.route('/MEDAILLES', {
  name: 'Medailles',
  action() {
    BlazeLayout.render('titre_médailles', { main: 'liste_des_médailles' });
  },
});


// Route pour le classement
FlowRouter.route('/CLASSEMENT', {
  name: 'Classement',
  action() {
    BlazeLayout.render('CLASSEMENT');
  },
});
*/
